import 'package:flutter/material.dart';
import 'package:paws/utils/helpers/color_helper.dart';

class CustomColors {
  static MaterialColor primarySwatch = createMaterialColor(Color(0xFF9E181C));
  static MaterialColor scaffoldBackgroundColor =
      createMaterialColor(Color(0xFFE3DFF2));
  static Color primary = createMaterialColor(Color(0xFF9E181C)).shade500;
}
